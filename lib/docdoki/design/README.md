# Design backups

Frozen design-phase references, kept for provenance. Not read at runtime and not
maintained — the shipped design lives in `SKILL.md`, `references/`, and the panel docs
under `panel/`.

- `prior-art.md` — the pre-build survey of adjacent tools and approaches.
- `dell-1996.DESIGN.md` — the verbatim vendored visual spec the panel adapts
  (`npx getdesign@latest add dell-1996`); the live contract is `panel/panel-design.md`.
